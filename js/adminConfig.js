/* ============================================================
   CYBER GRID :: ADMIN ACCESS PIN
   ------------------------------------------------------------
   Change this before you deploy / host the game.
   This is a LIGHT deterrent only (keeps casual players from
   poking at the control panel on the shared screen) — it is
   NOT real security. The PIN check happens entirely in the
   browser, so anyone who reads this file can see it. Do not
   use this game to manage anything sensitive.
   ============================================================ */

const ADMIN_PIN = "335837";
